akka {
  discovery {
    method = config

    config.services = {
      fake-service = {
        endpoints = [
          {
            host = "fake-service-host"
            port = 9119
          }
        ]
      }
    }
  }
}
